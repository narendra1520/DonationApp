<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.donationapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SelectNGOActivity">
        </activity>
        <activity android:name=".MainHome" />
        <activity android:name=".NgoListActivity" />
        <activity android:name=".CollListActivity">
        </activity>
        <activity android:name=".NewcollectionActivity" />
        <activity android:name=".ForgotpasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MoneyActivity" />
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CollDetailActivity" />
        <activity android:name=".NgoDetailActivity" />
        <activity android:name=".NGOFilter" />
    </application>

</manifest>